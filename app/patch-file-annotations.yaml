spec:
  template:
    metadata:
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/vault-service: "http://vault:8200"
        vault.hashicorp.com/agent-inject-secret-db-username: "secret/db-username:/vault/secrets/db-username"
        vault.hashicorp.com/agent-inject-secret-db-password: "secret/db-password:/vault/secrets/db-password"
